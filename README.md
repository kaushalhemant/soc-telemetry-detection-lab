# soc-telemetry-detection-lab
Real-time SOC detection engine that evaluates live telemetry against SIGMA rules, combining single-event matching with stateful sliding-window threshold correlation. Exports to Splunk SPL and Elastic queries, maps to MITRE ATT&amp;CK with Navigator export, and tracks precision, recall, FPR, and MTTD live.
